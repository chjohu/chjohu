name: Hello World

on:
  push:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: 인사 출력
        shell: bash
        run: |
          echo "안녕하세요, GitHub Actions!"
          echo "브랜치: ${{ github.ref }} / 유저: ${{ github.actor }}"

      - name: 현재 시간 출력
        shell: bash
        run: |
          echo "실행 시간: $(date)"
